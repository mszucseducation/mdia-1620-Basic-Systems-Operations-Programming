// Boolean
// Initialise the variable called proceed to the value true.

var proceed = true;
var i = 0;

while(proceed) {
    i++; // i = 1;
    proceed = false;
};

console.log(i);

// Data Type - Object
/**
 * 6-4-1 Create a variable called grannySmith 
 * and add the 
 * following values (use camelCase):
 * colour of the apple is green
 * origin location is australia
 * origin date is 1868
 * Then console log out the colour from 
 * the object.
 */
var grannySmith = {
    color: "green",
    originLocation: "australia",
    originDate: 1868
};

console.log(grannySmith);
