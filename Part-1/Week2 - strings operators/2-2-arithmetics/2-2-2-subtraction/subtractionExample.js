/**
 * Have a variable x equal to 4 and a variable y equal to 6.
 * Then subtract y from x and assign it to the variable z. 
 * Initialise and declare on the same line.
 * 
 */

var x = 4;
var y = 6;

var z = x - y;

console.log(z);


/**
 * 
 * We start with 15 laptops at our store. 
 * Then we sell 7 laptops. 
 * Have the first variable be initialLaptops and the second variable be sold. 
 * Then assign it to the variable available. Initialise and declare on the same line
 * 
 */

var initialLaptops = 15;
var sold = 7;

var available = initialLaptops - sold;

console.log(available);

// node subtractionExample.js
