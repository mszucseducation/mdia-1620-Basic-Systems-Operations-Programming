// Functions

/**Example 1
 * 
 * Declare and assign the variable x to a function called myFunction with the values 1 and 3. 
 * Multiply the two numbers. 
 * Console log the variable x afterwards.
 * 
 */

var x = myFunction(1,3);

function myFunction(first, second) {
    return first * second;
};

console.log(x);


/**Example 2
 * 
 * Declare and assign the variable addition to a function called addition function with the values 3 and 4. 
 * Add the two numbers. 
 * Console log the variable addition afterwards.
 * 
 */

var addition = additionFunction(3,4);

function additionFunction(varOne, varTwo) {
    return varOne + varTwo;
}

console.log(addition);
