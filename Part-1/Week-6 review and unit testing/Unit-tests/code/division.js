/**
 * Calculate the division 
 * 
 * @param {*} numOne the first value
 * @param {*} numTwo the second value
 * @return the division
 */
 function divisionFunction(numOne, numTwo) {
    return numOne / numTwo;
}

module.exports = {
    divisionFunction
};