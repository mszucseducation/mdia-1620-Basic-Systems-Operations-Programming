// Objects Data Type

/** Example 1
 * 
 * Create a variable called grannySmith and add the following values (use camelCase):
 * color of the apple is green
 * origin location is australia
 * origin date is 1868
 * 
 */

var grannySmith = {
    color: "green",
    originLocation: "australia",
    originDate: 1868
}

console.dir(grannySmith);
console.log(grannySmith);

console.log(grannySmith.color);


/** Example 2
 * 
 * Create a variable called summer bucket list and add the following values (use camelCase):
 * travel to has the value europe
 * local events has the value bbq
 * individual activity has the value biking
 * 
 * console.log out all the values
 * console.log out only the local events
 * 
 */

var summerBucketList = {
    travelTo: "europe",
    localEvents: "bbq",
    individualActivity: "biking"
}

console.log(summerBucketList);
console.log(summerBucketList.localEvents);
