// Number Data Types
/**
 * Declare a variable and initialise it to age. Then assign it to 26. Use camelCase.
 */

var age = 26;

/**
 * 
 * For the first one declare a variable and initialise it to x and assign it to 10. 
 * Then for the second one make it y then assign it to 2. 
 * Then create a final variable called total. Calculate the values of x divided by y. 
 * Then use console log to display the answer. Use camelCase.
 * 
 */

var x = 10;
var y = 2;
var total = x / y;

console.log(age)
console.log(total)