/**
 * Calculate the multiplication 
 * 
 * @param {*} numOne the first value
 * @param {*} numTwo the second value
 * @return the multiplied value
 */
function multiplicationFunction(numOne, numTwo) {
    return numOne * numTwo;
}

module.exports = {
    multiplicationFunction
};